import { StyleSheet } from "react-native";
import { Colors } from "./theme/Colors";

export const globalStyles = StyleSheet.create({
  primary: { fontSize: 55 },
  secondary: { fontSize: 40 },
  tertiary: { fontSize: 34 },
  paragraph: { fontSize: 24, color: Colors.LIGHT },
});
